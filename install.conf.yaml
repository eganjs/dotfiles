- shell:
  - [git submodule update --init --recursive, Installing submodules]

- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.vim: vim
    ~/.vimrc: vimrc
    ~/.config/i3/config: i3
    ~/.gitconfig: gitconfig
    ~/.Xresources: Xresources
